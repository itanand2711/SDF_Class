using System;

namespace operators
{
    class Program
    {
        static void Main(string[] args)
        {
            int a = 10;
            int b = 20;
            a++; //add 1 to value of a.
            b--;//removing value by 1.

            a = b;
            a > b;
            a < b;
            a >= b;
            a == b; //a equal to b compare two values.
            a = !b;//a is not equal to b.
        }
    }
}
